import React from 'react';

import { Navigation } from './Navigation';

export default {
  title: 'Frank/Navigation',
  component: Navigation,
};

export const Template = (args) => <Navigation {...args} />;